# DramaFlow 目前已实现

> 最后更新：2026-05-11

## 已打通的主链路

### 认证与协作

- JWT 登录、刷新、登出、忘记密码、重置密码、个人资料更新已可用。
- 登录支持 IP 维度速率限制（15 分钟内最多 10 次尝试）。
- 团队 CRUD、团队成员管理（添加/移除/角色变更）、团队邀请链接（创建/列表/吊销/查询/接受）已可用。
- 项目 CRUD、项目成员邀请、项目邀请接受、待处理邀请列表已可用。
- 平台管理员仪表盘、团队管理员仪表盘已可用。

### 剧本编辑器

- 纯文本剧本编辑器：场景 CRUD、对白管理、角色列表内联编辑（添加/移除/修改角色名和简介）。
- 富文本剧本编辑器：基于 TipTap，支持场景标题（SceneHeading）、对白块（DialogueBlock）、导演备注（DirectorNote）自定义扩展，结构化剧本编辑。
- 角色列表内联编辑，可直接在编辑器中增删角色。

### 分镜工作台

- 分镜编辑器已支持完整的镜头数据展示与编辑。
- 分镜工作台：按场景分组的镜头卡片列表，场景级别批量 TTS。
- **拖拽排序**：使用 dnd-kit 支持镜头卡片拖拽重新排序。
- **多选操作**：支持框选多个镜头进行批量操作。
- **侧边抽屉**：镜头详情以抽屉形式展示，带动画过渡。
- **媒体自动展示**：选中镜头时自动加载并展示已绑定的图片/视频。
- **镜头详情弹窗（三栏布局）**：
  - 左栏：可编辑元数据（景别、运镜、时长、描述等）+ 镜头导航 + 操作按钮
  - 中栏：媒体工作区（图片/视频 Tab 驱动切换、候选版本缩略图网格、生成操作）
  - 右栏：镜头内容（对白、音效、备注）+ TTS（音频播放+字幕预览）+ 提示词（联动媒体 Tab）
- **候选版本管理**：缩略图网格展示候选图片/视频，支持放大查看（Lightbox）和显式采纳指定版本。
- **关联提示词预览**：镜头详情弹窗中展示自动拼装的图片/视频生成提示词。
- 批量生成：支持对缺失图片/视频的镜头进行批量图片/视频生成，工具栏集成批量生成控制。
- 景别和运镜方式的中英文别名映射与本地化标签已完整。

### 文档、版本、审核与评论

- Script、Storyboard、World Bible、Image、Video、Audio、Subtitle 文档模型已统一进入版本体系。
- 版本创建、恢复、提审、审批、驳回已闭环。
- 草稿版本支持编辑内容与删除。
- 独立版本管理面板：左右分栏布局，左侧版本列表（支持状态过滤），右侧内容预览与版本对比。
- 审核策略已接入按内容类型的 `resolveContentReviewRequired()` 与 `canAutoApprove()`。
- 审核配置面板：按内容类型独立配置是否需要审核、可自动通过的角色列表。
- 项目级审核策略可独立配置（`inherit` / `required` / `bypass`）。
- 审核意见会写入审计记录；评论系统已支持 `parentId` 线程回复。

### AI 生成与媒体生产

- Synopsis、Script、Storyboard、Rewrite 已支持 SSE 流式生成（`/stream` 端点）。
- AI 生成面板支持"先生成大纲"流程，SSE 流式输出。
- 图片、视频支持单镜头生成与批量任务创建。
- 单镜头媒体支持多候选版本，并可显式采纳指定版本。
- Prompt 预览链路已接入世界观角色、地点、风格注入。
- Provider 选择器：生成时可选择使用个人或团队的图片/视频 Provider。
- 世界观参考图 AI 生成：支持为角色、地点、风格指南生成 AI 参考图。

### 多 Provider 支持

- 已接入 7 个 AI Provider 实现：
  - **文本生成**：OpenAI 兼容 completions（支持流式）。
  - **图片生成**：OpenAI 兼容、Google Gemini（文生图 + 参考图编辑）、Stable Diffusion WebUI、ComfyUI（工作流模式）、Grok。
  - **视频生成**：OpenAI 兼容、Grok。
  - **TTS**：语音合成（含 Mock 适配器）。
- Provider 配置表单：通用的 Provider 添加/管理表单，支持配置各 Provider 专用参数（SD 采样器/步数、ComfyUI 工作流 JSON、Grok 宽高比/时长等）。
- 用户和团队均可独立管理多套图片/视频 Provider 配置，并指定默认 Provider。

### 媒体绑定与字幕

- 分镜内容中的 `mediaBindings` 支持每个镜头独立绑定图片版本、视频版本、音频版本和字幕。
- 媒体与字幕已集成到分镜版本管理流程中。
- 草稿版本的媒体绑定可独立更新。

### TTS

- TTS 任务会读取 World Bible 中保存的角色音色配置，而不是硬编码默认音色。
- 已提供角色音色配置 UI、音色样例播放、单镜头 TTS、按场景批量 TTS。
- 批量 TTS 完成后会为对应镜头生成 Audio 文档与版本。

### 时间线编辑器

- 可视化时间线编辑器：视频、对白、音乐、音效、字幕五类轨道。
- 缩放控制（20-200 像素/秒），播放/暂停控制。
- 从媒体库拖拽素材到时间线轨道。
- 自动组装：根据分镜数据自动填充时间线。
- 保存时间线。
- 导出为 MP4/MOV/WebM，FFmpeg 可用时执行真实导出。
- 导出能力检测端点（`GET /export/capabilities`）。

### 媒体库

- 侧边媒体库面板：视频、音频、字幕、图片四个标签页。
- 资产卡片展示、预览、上传功能。
- 字幕文本预览。
- 支持注册项目资产。

### 世界观编辑器

- 角色档案管理（外观、性格、标签、参考图、服装）。
- 场景地点管理（描述、光照、时间段、参考图）。
- 视觉风格指南（风格描述、色彩方案、构图备注、负面提示词、参考图）。
- 角色音色配置（TTS Provider、语音 ID、语速/情感/音量参数）。
- AI 参考图生成对话框：支持为角色、地点、风格指南生成 AI 参考图。

### 通知与实时更新

- 通知中心、未读计数、标记已读/全部已读已可用。
- WebSocket 已推送 `job.updated`、`review.updated`、`notification.created`，轮询继续保留为降级路径。

### 国际化

- 工作区面板和 Provider 表单支持中文/英文切换。
- 景别和运镜方式已完整支持中文/英文本地化标签。

## 前端路由结构

- 公开路由：首页、登录、忘记密码、重置密码、团队邀请接受、项目邀请接受
- 受保护路由：
  - `/dashboard` — 项目列表
  - `/dashboard/platform` — 平台管理后台
  - `/dashboard/team` — 团队管理
  - `/dashboard/team/settings` — 团队设置
  - `/dashboard/settings/profile` — 个人设置
  - `/dashboard/settings/language` — 语言设置
  - `/dashboard/notifications` — 通知页
- 项目路由：
  - `/projects/:projectId/workspace` — 统一工作区（支持 `?mode=` 参数）
  - `/projects/:projectId/generate` — AI 生成
  - `/projects/:projectId/review` — 审核面板
  - `/projects/:projectId/drafts` — 草稿管理

### 工作区 Mode

统一工作区支持以下模式（通过 `?mode=` URL 参数切换）：

- `info` — 项目信息面板
- `document` — 文档模式（含 view / edit / generate / versions 子 tab；worldbible 也归入此模式）
- `tasks` — 任务面板
- `timeline` — 时间线编辑器

兼容映射：`generate` → document.generate、`worldbible` → document.worldbible、`media` → document.view。

## 当前接口形态

### 认证

- `POST /auth/register` — 用户注册
- `POST /auth/login` — 登录（含速率限制）
- `POST /auth/refresh` — 刷新令牌
- `POST /auth/logout` — 登出
- `POST /auth/forgot-password` — 发起密码重置
- `POST /auth/reset-password` — 执行密码重置
- `GET /auth/me` — 获取当前用户资料
- `PATCH /auth/me` — 更新个人资料（含 LLM 配置、多 Provider 配置、默认 Provider）
- `POST /auth/me/llm-models` — 列出可用 LLM 模型

### 团队

- `GET /teams` — 列出用户所在团队
- `GET /teams/:id` — 获取团队详情
- `POST /teams` — 创建团队
- `PATCH /teams/:id` — 更新团队（名称、审核策略、LLM 配置、图片生成配置）
- `DELETE /teams/:id` — 删除团队
- `POST /teams/:id/llm-models` — 列出团队可用 LLM 模型
- `POST /teams/:id/members` — 添加团队成员
- `DELETE /teams/:teamId/members/:memberId` — 移除团队成员
- `PATCH /teams/:teamId/members/:memberId` — 变更成员角色
- `POST /teams/:id/invite-links` — 创建团队邀请链接
- `GET /teams/:id/invite-links` — 列出团队邀请链接
- `DELETE /teams/:teamId/invite-links/:linkId` — 吊销邀请链接
- `GET /invite-links/:token` — 查询邀请链接信息
- `POST /invite-links/:token/accept` — 接受团队邀请

### 项目

- `GET /projects` — 列出用户参与的项目
- `POST /projects` — 创建项目
- `GET /projects/:id` — 返回工作区 summary
- `PATCH /projects/:id` — 更新项目
- `DELETE /projects/:id` — 删除项目
- `PATCH /projects/:id/review-policy` — 更新项目审核策略
- `POST /projects/:id/invites` — 邀请项目成员
- `POST /projects/:id/members` — 添加项目成员
- `GET /project-invites/pending` — 列出待处理项目邀请
- `POST /project-invites/:id/accept` — 接受项目邀请

### 文档与版本

- `GET /projects/:id/versions` — 按需拉取项目版本数据（支持分页）
- `GET /documents/:id/versions` — 列出文档版本（支持分页）
- `POST /documents/:id/versions` — 创建版本
- `PATCH /versions/:id` — 更新草稿版本内容
- `DELETE /versions/:id` — 删除草稿版本
- `POST /documents/:id/adopt-version` — 采纳指定版本为基线
- `POST /versions/:id/adopt` — 采纳指定版本
- `POST /versions/:id/submit` — 提交版本
- `POST /versions/:id/advance-to-review` — 推进到审核
- `POST /versions/:id/approve` — 批准版本
- `POST /versions/:id/reject` — 驳回版本
- `POST /versions/:id/restore` — 恢复版本
- `PATCH /versions/:id/media-binding` — 更新草稿版本的媒体绑定

### 评论

- `GET /versions/:id/comments` — 列出版本评论
- `POST /versions/:id/comments` — 添加评论（支持线程回复）

### 世界观

- `GET /projects/:id/world-bible` — 获取世界观设定
- `PATCH /projects/:id/world-bible` — 更新世界观设定
- `POST /projects/:id/world-bible/characters` — 添加角色
- `PATCH /projects/:projectId/world-bible/characters/:characterId` — 更新角色
- `DELETE /projects/:projectId/world-bible/characters/:characterId` — 删除角色
- `POST /projects/:id/world-bible/locations` — 添加地点
- `PATCH /projects/:projectId/world-bible/locations/:locationId` — 更新地点
- `DELETE /projects/:projectId/world-bible/locations/:locationId` — 删除地点
- `PATCH /projects/:id/world-bible/style-guide` — 更新视觉风格指南
- `PATCH /projects/:projectId/world-bible/characters/:characterId/voice` — 更新角色语音配置

### 审核配置与记录

- `GET /projects/:id/audit-configs` — 获取审核配置
- `PATCH /projects/:id/audit-configs/:contentType` — 更新审核配置
- `GET /projects/:id/audit-records` — 列出项目审核记录（支持类型过滤和分页）
- `GET /versions/:id/audit-records` — 列出版本审核记录

### AI 生成任务

- `POST /projects/:id/script-jobs` — 创建剧本生成任务
- `POST /projects/:id/script-jobs/stream` — SSE 流式剧本生成
- `POST /projects/:id/synopsis-jobs` — 创建大纲生成任务
- `POST /projects/:id/synopsis-jobs/stream` — SSE 流式大纲生成
- `POST /projects/:id/storyboard-jobs` — 创建分镜生成任务
- `POST /projects/:id/storyboard-jobs/stream` — SSE 流式分镜生成
- `POST /projects/:id/rewrite-jobs` — 创建改写任务
- `POST /projects/:id/rewrite-jobs/stream` — SSE 流式改写
- `POST /shots/:id/image-jobs` — 创建图片生成任务
- `POST /shots/:id/video-jobs` — 创建视频生成任务
- `POST /shots/:id/tts-jobs` — 创建 TTS 任务
- `POST /scenes/:id/batch-tts-jobs` — 按场景批量创建 TTS 任务
- `POST /projects/:id/batch-image-jobs` — 批量图片生成任务
- `POST /projects/:id/batch-video-jobs` — 批量视频生成任务
- `GET /batch-jobs/:batchId` — 查询批量任务状态
- `POST /shots/:id/preview-prompt` — 预览图片生成提示词
- `POST /shots/:id/preview-video-prompt` — 预览视频生成提示词

### 世界观参考图生成

- `POST /projects/:projectId/world-bible/characters/:characterId/generate-reference-image` — 生成角色参考图
- `POST /projects/:projectId/world-bible/locations/:locationId/generate-reference-image` — 生成地点参考图
- `POST /projects/:projectId/world-bible/style-guide/generate-reference-image` — 生成风格指南参考图

### 任务管理

- `GET /projects/:id/jobs` — 列出项目任务（支持状态/类型/batchId 过滤和分页）
- `GET /jobs/:id` — 获取任务详情
- `POST /jobs/:id/cancel` — 取消任务
- `POST /jobs/:id/retry` — 重试失败任务

### 时间线与导出

- `GET /projects/:id/timeline` — 获取时间线
- `PUT /projects/:id/timeline` — 保存时间线
- `POST /projects/:id/timeline/auto-assemble` — 自动组装时间线
- `GET /export/capabilities` — 检测导出能力（FFmpeg 是否可用）
- `POST /projects/:id/export-jobs` — 创建导出任务
- `GET /projects/:id/exports` — 列出导出记录

### TTS

- `GET /tts/voices` — 列出可用 TTS 音色

### 存储

- `POST /uploads` — 创建上传目标（直传流程）
- `PUT /uploads/direct/:key` — 直接上传文件
- `GET /assets/:id/url` — 获取资产访问 URL
- `POST /projects/:id/assets` — 注册项目资产

### 通知

- `GET /notifications` — 列出通知（支持仅未读过滤和分页）
- `GET /notifications/unread-count` — 获取未读数
- `PATCH /notifications/:id/read` — 标记已读
- `POST /notifications/mark-all-read` — 全部标记已读

### 管理后台

- `GET /admin/platform/overview` — 平台级概览指标
- `GET /admin/teams/:id/overview` — 团队管理仪表盘
- `GET /admin/teams/:id/settings` — 团队设置（LLM、图片生成配置）

### WebSocket 事件

- `job.updated` — 任务状态更新
- `review.updated` — 审核状态更新
- `notification.created` — 新通知

### Worker 内部接口

- `GET /internal/jobs/next` — 领取下一个待处理任务（按优先级排序）
- `POST /internal/jobs/:id/process` — 执行任务
- `POST /internal/jobs/:id/retry` — 重试失败任务（系统级）

## 当前运行前提

- 运行时仍然使用 `DevDatabaseService` 的 JSON 存储。
- Worker 仍然通过内部 API 轮询领取任务（可配置轮询间隔），并未迁移到 BullMQ。
- Prisma schema 已对齐到当前共享契约的大部分核心模型，但仍是目标生产模型，不是 live path。

## 仍处于开发态、尚未生产化的部分

- Prisma 还没有接入真实运行时读写。
- Redis / BullMQ 还没有替代当前轮询 Worker。
- Web 端与 Worker 端还没有独立测试脚本；当前测试重点仍是 API 与 shared。
- 测试覆盖：shared 包业务规则测试、数据库初始化测试、Provider 测试（文本/媒体/Gemini 共 9 个用例），整体测试覆盖仍较薄。

## 下一阶段更适合继续推进的方向

1. 将队列抽象从"轮询 Worker"平滑迁移到 BullMQ，同时保持现有 API 边界不变。
2. 把 Prisma 接到 live path，并补迁移脚本与真实数据库回归验证。
3. 继续补强可观测性，例如任务耗时、provider 调用日志、导出耗时和失败原因聚合。
4. 针对时间线编辑器补拖拽裁剪、轨道级精细编辑与更完整的预览能力。
5. 扩充测试覆盖，特别是 Web 端组件测试和 Worker 端 Provider 集成测试。
